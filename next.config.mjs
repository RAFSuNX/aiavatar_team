/** @type {import('next').NextConfig} */
const nextConfig = {
    output: "export", // Ensures static HTML files are generated
    distDir: "out",   // Sets the output directory for static files
  };
  
  export default nextConfig;
  